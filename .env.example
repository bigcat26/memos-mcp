# Memos MCP Server Configuration

# Memos instance URL (e.g., https://memos.example.com)
MEMOS_BASE_URL=https://your-memos-instance.com

# Memos API access token (get from Settings > My Account > Create Access Token)
MEMOS_ACCESS_TOKEN=your_access_token_here

# Optional: Custom API prefix (default: /api/v1)
MEMOS_API_PREFIX=/api/v1

# Optional: Request timeout in seconds (default: 30)
MEMOS_TIMEOUT=30

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO